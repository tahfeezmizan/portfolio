let text = "Loading...";
        let rings = 2;
        let ringSectors = 30;

        .preloader
        for (let r = 0; r < rings; ++r) {
		    .preloader__ring
        for (let s = 0; s < ringSectors; ++s) {
				.preloader__sector=text[s] || ""
         }
        }